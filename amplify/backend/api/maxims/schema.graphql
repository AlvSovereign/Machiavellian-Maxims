type Maxim @model {
	id: ID!
	name: String!
	maxim: String!
}

input CreateMaximInput {
	id: ID
	name: String!
	maxim: String
}

type Mutation {
	batchAddMaxims(maxims: [CreateMaximInput!]): [Maxim]
}
